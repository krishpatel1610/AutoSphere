<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <script>
        var status = `<%- JSON.stringify(status) %>`;
        if (JSON.parse(status) == 'verified') {
            if (confirm("Do you want to Login?")) {
                window.location.href = "http://localhost:3000/Admin";
            }
        }
    </script>

    <!-- Include Ant Design CSS (adjust the path as per your setup) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/antd/4.16.13/antd.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f2f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            width: 300px;
        }
        .container h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #5214ae;
        }
        .container form {
            display: flex;
            flex-direction: column;
        }
        .container form .form-group {
            margin-bottom: 10px;
        }
        .container form input {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }
        .container form input[type="submit"] {
            background-color: #5214ae;
            color: #fff;
            border: none;
            cursor: pointer;
            font-size: 16px;
        }
        .container form input[type="submit"]:hover {
            background-color: #40a9ff;
        }
        .container .login-link {
            text-align: center;
            margin-top: 10px;
        }
        .container .login-link a {
            color: #5214ae;
            text-decoration: none;
        }
        .bg-blue {
            background: linear-gradient(to right, #8971ad, #5d26af, #7350a9); 
        }
        .skew-box {
            transform: skewY(-6deg);
        }
        .unskew-box {
            transform: skewY(0) rotate(-6deg);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Reset Password</h1>
        <form action="" method="POST">
            <div class="form-group">
                <label for="password" class="form-label">New Password</label>
                <input type="password" name="password" id="password" placeholder="New Password" required/>
            </div>
            <div class="form-group">
                <label for="confirm-password" class="form-label">Confirm Password</label>
                <input type="password" name="confirm-password" id="confirm-password" placeholder="Confirm Password" required/>
            </div>
            <input type="submit" value="Submit"/>
        </form>
        <div class="login-link">
            Remembered your password? <a href="http://localhost:3000/Admin/">Login</a>
        </div>
    </div>
</body>
</html>
